export declare type Range = [number, number];
export declare function normalize_ranges(ranges: Range[]): [number, number][];
export declare function build_regex(ranges: Range[], flag?: string): RegExp;
